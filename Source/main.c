#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include "utils.h"
#include "process.h"
int main(){
    shLoop();
    /*
    char * str = readLine();
    printf("%s\n",str);
    int mode;
    char ** args = parseArgs(str,&mode);
    printf("%d\n",mode);
    while (*args)
    {
        printf("%s\n",*args);
        args++;
    }*/
    return 0;
}